<html>
<head>
    <title>Message Board</title>
    <style>
        body {
            font-family: Arial, Helvetica, sans-serif;
            background-color: #eee;
            color: #333;
            max-width: 800px;
            margin: auto;
            padding: 30px;
        }
    </style>
</head>
<body>

    {% for texts in text %}
        <p>{{texts.author}}: {{texts.text}}</p>
    {% endfor %}
    {% if not user %}
       
        <a href="/login">Log In</a>
        <a href="/register">Register</a>
    {% else %}
        <form method="POST" action="message">
            <input style="width: 500px" type="text" placeholder="enter a message" name="message" />
            <input type="submit" value="submit" />
        </form>
        <a href="/logout">Log out</a>
    {% endif %}

</body>
</html>